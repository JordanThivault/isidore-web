import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Rien de particulier pour l'instant : site statique + une Server Action.
  // Ajoute ici tes options plus tard (images distantes, redirections, etc.).
};

export default nextConfig;
